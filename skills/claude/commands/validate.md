Deprecated. Prefer `/finish`.

Run:

```powershell
.\scripts\ai.ps1 validate $ARGUMENTS
```

This remains as a compatibility alias for marking validation without commit. For normal final closure, use `/finish`.

Then repeat the exact `NOME DO CHAT: ...` line and run `/rename <suggested-title>` if Claude supports it.
