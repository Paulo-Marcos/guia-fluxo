---
name: feature
description: Shim for ai-process. Use when the user invokes $feature or asks to start/register a new feature.
---

# Feature Shim

Call the core process script:

```powershell
.\scripts\ai.ps1 feature "<title>" --context "<context>"
```

Then continue using `ai-process`.
